function mostrar() {
    var tipo = document.getElementById("password");

    if(tipo.style == 'password') {
        tipo.type = 'text';
    } else {
        tipo.type = 'password';

    }
}

function mostrar() {
    var tipo = document.getElementById("password");

    if(tipo.style == 'password') {
        tipo.type = 'text';
    } else {
        tipo.type = 'password';

    }
}

